const assert = require('chai').assert;

// test suite is wired up but is not testing anything

  describe('the server exists', function(){
    context('find server', function(){
      it('should return true', function(){
        assert.equal();
      });
    });
  });
